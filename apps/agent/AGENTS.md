# Python Virtual Environment
- **Always** ensure the use of the Python virtual environment (`.venv`) when interacting with the python code, installing dependencies, or running tests.

# Running the App
- Use `./run.sh` to execute the application locally. It automatically setups the virtual env, installs/updates dependencies from `pyproject.toml`, and runs the agent module (`berg10_agent`).